import React from 'react';

export default function NavBar() {
    return (
        <div className="nav">
            <div className="nav-cont">
                <a href="#"> Home</a>
                <a href="#"> About</a>
                <a href="#"> Contact</a>
            </div>
        </div>
    );

}